# 2026-09-01 — corridor ML fix committed + pushed (not yet deployed)

## Context
This is the liveflights half of the "demo reliability" problem: during the Ruchita Design
on-site (29 Aug), the corridors layer was live but broken (26 sparse/disconnected corridors, one
holding 23% of all traffic). The actual fix was done and verified locally on 2026-08-31 (see
`~/.claude/projects/-Users-uttkarshtyagi-job/memory/liveflights_full_context.md` section 14 for
the full root-cause writeup) but was sitting uncommitted in the working tree. Today's session
picked that up specifically because the user asked to fix the liveflights + streampulse demo
bugs together.

## What happened today
1. Checked AWS credentials first (`aws sts get-caller-identity`) — still `ExpiredToken`, same
   AWS Academy Lab session-token issue noted on 2026-08-31. Can't touch AWS this session.
2. Reviewed all 13 modified files + new `ml/scratch/` directory against the memory file's
   description of the fix — matched, no surprises.
3. Added `.gitignore` entries for `ml/scratch/artifacts/` (regenerable model binaries/plots/test
   json — the scripts that produce them are committed, the outputs aren't) and
   `web/tsconfig.tsbuildinfo` (build cache, shouldn't be tracked).
4. Excluded `.claude/settings.local.json` from the commit (personal, local-only permissions
   tweak, not meant to be shared).
5. Committed everything else as one logical commit (`4e5b72b`) with a full explanation of the
   two DBSCAN bugs (raw heading instead of sin/cos; one fixed eps across too-wide a grid cell),
   the binned-mean polyline fix, the airport-snapping feature, and the Baltic/Balkans coverage
   gap fix. **Pushed to `origin/main` clean.**

## Current state — what's committed vs. deployed
- **Committed + pushed**: all corridor-discovery code (`ml/scratch/train_all.py` and friends),
  `AirportLayer.tsx`, `CorridorLayer.tsx` airport tooltips/spur lines, `LayerControls.tsx`
  disclaimer copy, `api/cloud/app.py` (serves the corridors artifact + models/* IAM read),
  `infra/terraform/iam.tf` (models/* S3 read permission), `infra/terraform/variables.tf` (2 new
  `adsb_lol_points` for Baltic states + Balkans interior), `ingestion/airports.py` (+6 airports).
- **NOT deployed** — three separate things still need real AWS access:
  1. Upload the fresh `ml/scratch/artifacts/corridors.json` (1,150 corridors) to the live S3
     bucket at the path `api/cloud/app.py` actually reads from (`models/corridors.json` per the
     iam.tf comment — confirm the exact key before uploading).
  2. `terraform apply` in `infra/terraform/` to actually create the 2 new `adsb_lol_points` +
     the `models/*` IAM read permission — until this runs, real ingestion never starts covering
     the Baltic/Balkans-interior gap, and the API Lambda's IAM role can't read `models/*` yet
     even after step 1's upload.
  3. Rebuild + redeploy the `web/` Next.js static export to S3 so `AirportLayer.tsx` and the
     updated `CorridorLayer.tsx` (airport tooltips, dashed hub-spur lines) actually render live —
     the current live bundle predates this commit entirely.
- Until all three happen, **the live dashboard still serves the old buggy 26-corridor artifact**
  — don't call this demo-ready based on this session's work alone.

## Next session, once AWS credentials are refreshed
Read this file + `liveflights_full_context.md` section 14 first, then: upload the corridors
artifact, `terraform apply`, rebuild+deploy the web bundle, and verify live in a browser
end-to-end (not just "the deploy command succeeded") before updating
`~/.claude/projects/.../memory/project_demo_reliability_gap.md` to say liveflights is actually
fixed.
