fastapi==0.115.6
mangum==0.19.0
boto3==1.35.90
# Pinned to exactly what trained the models/*.joblib artifacts
# (ml/scratch/train_all.py) -- scikit-learn's pickle format is not
# guaranteed compatible across versions, so this must move in lockstep
# with whatever trains new artifacts.
numpy==2.5.1
scikit-learn==1.9.0
joblib==1.5.3
